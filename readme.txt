you can use such following command
cd ~/Desktop
mkdir name
cd name
git init(funding the store)
git touch(add the file)
git add(zancun area)
git status -sb
git add .
git commit -m""
git remote add origin https://github.com/username/git-demo.git
git branch -M main
git push -u origin main
cat <filename>(look up)
vi <filename>(write)i-enter esc+:wq-quit
1.git log --prettty=oneline
2.git reset --hard HEAD^
or git reflog
delete:git rm <filename>
git checkout(restore)
thanks
